# {name} — CAPTAIN project context

Generated by [create-captain](https://www.npmjs.com/package/create-captain). Use this file to onboard humans and coding agents.

## Stack

| Setting | Value |
|---------|-------|
| Topology | `{topology}` |
| Apps | `{apps}` |
| Scope | `{scope}` |
| Package manager | `{packageManager}` |
| Backend | `{backend}` |
| Convex example | {convexExample} |
| Auth | `{auth}` |
| Web i18n | `{i18nWeb}` |
| Mobile i18n | `{i18nMobile}` |
| Web UI | `{uiWeb}` |
| Mobile UI | `{uiMobile}` |
| Locales | `{locales}` (default: `{defaultLocale}`) |
| Expo runtime | `{runtime}` |

## Layout

- Standalone projects keep routes in `src/app`, shared code in `src/lib`, and
  provider wiring in `src/integrations`.
- Monorepos keep applications in `apps/*` and shared code in `packages/*`.

## Commands

Use `{packageManager}` to install dependencies and run the generated
`typecheck`, `lint`, `build`, and `dev` scripts.

## Agent notes

- Config source of truth: `project.json`
- Compatibility matrix: see create-captain `compatibility.json`
- CAPTAIN emits only integrations recorded in the resolved project config.
- Import generated modules by their file or package subpath; CAPTAIN does not generate barrel files.
{backendNotes}
