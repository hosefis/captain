# {name} — CAPTAIN project context

Generated by [create-captain](https://www.npmjs.com/package/create-captain). Use this file to onboard humans and coding agents.

## Stack

| Setting | Value |
|---------|-------|
| Topology | `{topology}` |
| Apps | `{apps}` |
| Scope | `{scope}` |
| Backend | `{backend}` |
| Auth | `{auth}` |
| Web i18n | `{i18nWeb}` |
| Mobile i18n | `{i18nMobile}` |
| Web UI | `{uiWeb}` |
| Mobile UI | `{uiMobile}` |
| Locales | `{locales}` (default: `{defaultLocale}`) |
| Expo runtime | `{runtime}` |
| Modules | `{modules}` |

## Layout

- `apps/*` — application entrypoints (Next.js, Expo, desktop slot)
- `packages/core` — shared domain types and module interfaces (`{scope}/core`)
- `packages/adapters-*` — stack-specific adapter implementations

## Commands

```bash
pnpm install
pnpm typecheck
pnpm lint
pnpm dev
```

## Agent notes

- Config source of truth: `project.json`
- Compatibility matrix: see create-captain `compatibility.json`
- Phase 2 modules/adapters beyond workspace skeleton are applied incrementally by CAPTAIN recipes
